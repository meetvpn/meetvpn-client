import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'meetvpn.client',
  appName: 'meetvpn-client',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
