import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.meetvpn.app",
  appName: "MeetVPN",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
